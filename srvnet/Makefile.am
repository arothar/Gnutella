bin_PROGRAMS = srvnet
srvnet_SOURCES = srvnet.c srvnet-proc.c Genericas.c

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
srvnet_LDFLAGS = $(all_libraries) 
noinst_HEADERS = srvnet-proc.h srvnet.h Genericas.h
